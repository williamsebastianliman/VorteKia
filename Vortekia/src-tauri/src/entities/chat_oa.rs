//! `SeaORM` Entity, @generated by sea-orm-codegen 1.1.5

use sea_orm::entity::prelude::*;
use serde::{Deserialize, Serialize};

#[derive(Clone, Debug, PartialEq, DeriveEntityModel, Eq, Serialize, Deserialize)]
#[sea_orm(table_name = "chat_oa")]
pub struct Model {
    #[sea_orm(primary_key)]
    pub chat_id: i32,
    pub sender_staff_id: String,
    pub oa_id: String,
    pub message: String,
    pub is_reply: i8,
    pub timestamp: DateTime,
}

#[derive(Copy, Clone, Debug, EnumIter, DeriveRelation)]
pub enum Relation {
    #[sea_orm(
        belongs_to = "super::official_account::Entity",
        from = "Column::OaId",
        to = "super::official_account::Column::OaId",
        on_update = "Cascade",
        on_delete = "Cascade"
    )]
    OfficialAccount,
    #[sea_orm(
        belongs_to = "super::staff::Entity",
        from = "Column::SenderStaffId",
        to = "super::staff::Column::StaffId",
        on_update = "Cascade",
        on_delete = "Cascade"
    )]
    Staff,
}

impl Related<super::official_account::Entity> for Entity {
    fn to() -> RelationDef {
        Relation::OfficialAccount.def()
    }
}

impl Related<super::staff::Entity> for Entity {
    fn to() -> RelationDef {
        Relation::Staff.def()
    }
}

impl ActiveModelBehavior for ActiveModel {}
